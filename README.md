# MI Tensor Transform GPU kernels

GPU kernels used in TensorTransform.

Using [res_embed](https://github.com/dmikushin/res_embed) to embed GPU kernel sources in a cross-platform way.

## Building

```
mkdir build
cd build
cmake ..
make -j12
```

